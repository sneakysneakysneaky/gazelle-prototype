import { Component } from 'react';
import ReactMixin from 'react-mixin';

export default class ManageUserClasses extends Component {

    state = {
        hideCompleted: false
    }

    render() {
        return (
            <div>
                manage user classes
            </div>
        );
    }
};